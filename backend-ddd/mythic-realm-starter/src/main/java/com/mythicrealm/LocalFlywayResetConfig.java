package com.mythicrealm;

/**
 * Compatibility placeholder for IDE incremental builds that may still carry
 * the old Flyway reset class in target/classes.
 */
final class LocalFlywayResetConfig {
    private LocalFlywayResetConfig() {
    }
}
